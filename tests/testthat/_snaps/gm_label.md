# create_label, update_label, update_label_patch and delete_label work

    Code
      gm_label(new_label3$id)
    Condition
      Error in `gmailr_GET()`:
      ! Gmail API error: 404
        Requested entity was not found.

